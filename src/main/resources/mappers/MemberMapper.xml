<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.damoim.member.repository.MemberMapper">
    <insert id="save">
        INSERT INTO tb_member
            (member_id, member_password, member_name,
        member_phone, member_nickname, member_address)
        VALUES
            (#{memberId}, #{memberPassword}, #{memberName}, #{memberPhone}, #{memberNickname}, #{memberAddress})
    </insert>

    <select id="findOne" resultType="com.project.damoim.member.entity.Member">
        SELECT
            member_id, member_password, member_name,
            member_phone, member_nickname, member_address
        FROM tb_member
    </select>
</mapper>